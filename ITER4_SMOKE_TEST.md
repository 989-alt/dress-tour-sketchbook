# Dress Tour Sketchbook — 전체 스모크 테스트 체크리스트

빌드 후 브라우저에서 수동으로 확인 (`npm run build && npm run preview`).

---

## Iter 1: 기초 기능

- [ ] `/` 페이지에서 "베이스 사진 업로드 + 포즈 검출" 버튼 클릭 → 파일 선택 → 모달이 검출 단계로 전환
- [ ] MediaPipe 모델이 없으면 "수동 배치로 진행" 옵션 표시
- [ ] 사진 업로드 완료 후 `/` 페이지에서 사진 썸네일이 표시되고 "+ 새 드레스 추가" 버튼 활성화
- [ ] `/new` 경로로 이동 시 새 드레스 엔트리가 생성되고 편집 화면 진입
- [ ] 편집 화면에서 좌측 캔버스가 사진 + 합성 SVG를 함께 표시
- [ ] 실루엣 탭 → 9종 칩 중 다른 실루엣 선택 시 좌측 캔버스 갱신
- [ ] 앵커 탭 → 13 앵커가 사진 위에 도트로 표시, 드래그로 이동 가능
- [ ] 앵커 탭 → 수동 모드 토글 시 5점으로 전환
- [ ] 펜 탭 → 펜 스케치 굵기 / 색 / 지우개 작동, undo / clear 작동
- [ ] 기본 탭 → 별명, 매장, 드레스#, 조명 메모 입력 후 새로고침 시 유지
- [ ] 메모 탭 → 5 카테고리 체크박스, 인용 2개, 4 별점, 장점·단점 입력 가능
- [ ] `/` 페이지에서 EntryCard 클릭 → 해당 엔트리 편집 화면으로 이동
- [ ] `/` 페이지에서 "요약 보기" → `/summary` 페이지 이동, 정렬 기능 작동
- [ ] `/summary` 페이지에서 카드 클릭 → 상세 모달 열림, Esc / 백드롭으로 닫기
- [ ] 새로고침 시 데이터 유지 (IndexedDB 영속)

---

## Iter 2: 넥라인 / 소매 / 보디스 / 등판 / 원단 / 컬러

- [ ] 넥라인 탭 → 15종 칩, 각 칩 hover 시 한국어 설명 tooltip 표시
- [ ] 소매 탭 → 10종 소매 + 4종 소재 칩 선택, 캔버스 갱신
- [ ] 보디스 탭 → 허리 위치 5종 · 보디스 구조 4종 · 허리 액센트 5종 · 색상 9종 선택
- [ ] 등판 탭 → 8종 등판 스타일 선택; openBack 선택 시 개방 깊이 슬라이더 표시
- [ ] 원단 탭 → 상의/치마/소매/베일 각각 7종 원단 칩, 독립 선택 가능
- [ ] 컬러 탭 → 9종 주색 선택; 옴브레 토글 시 보조색 선택 가능

---

## Iter 3: 스커트 / 장식

- [ ] 스커트 탭 → 텍스처 8종, 티어드 선택 시 단 수 표시, 슬릿 3종 + 높이, 트레인 5종
- [ ] 장식 탭 → "+ 장식 추가" 클릭 → 종류 9종 선택 → 부위 6종 선택 후 카드 생성
- [ ] 장식 카드 → 강도 슬라이더, threeDFlorals 크기, embroidery 스타일, ribbons 위치/수량, decorativeButtons 면/수량 작동
- [ ] 장식 카드 → 강도 0 설정 시 카드 반투명, × 버튼으로 삭제

---

## Iter 4: 베일 / 헤어 / 비교 / 내보내기 / PWA

- [ ] 베일 탭 → 길이 7종 선택; 베일 있을 때 가장자리 4종 + 레이어 2종 표시
- [ ] 헤어 탭 → 액세서리 6종 선택, SVG 미리보기 표시
- [ ] `/summary` → "비교 모드" 버튼 클릭 → 드레스 2개 선택 → 비교 모달 열림
- [ ] 비교 모달 → 좌우 캔버스, 별점, 장단점 나란히 표시; 닫기 작동
- [ ] `/summary` → 즐겨찾기(핀) 클릭 시 카드 상단 고정
- [ ] `JSON 받기` 클릭 → JSON 파일 다운로드
- [ ] `JSON 복원` 클릭 → 파일 선택 → 확인 후 데이터 복원
- [ ] `/summary` → PNG 내보내기 버튼 → PNG 파일 다운로드 (브라우저 전용)

### PWA / 오프라인

- [ ] `npm run build` 성공, `dist/sw.js` 파일 존재
- [ ] `dist/manifest.webmanifest` 존재, `lang: "ko"` 포함
- [ ] `npm run preview` → 브라우저 네트워크 탭 "오프라인" 전환 → 새로고침 → 앱 정상 로드
- [ ] 모바일 Chrome → "홈 화면에 추가" 옵션 표시 (PWA 설치 가능)

### 기타 (Iter 4)

- [ ] 실루엣 칩 hover 시 한국어 설명 tooltip 표시 (title 속성)
- [ ] 원단 칩, 트레인 칩, 베일 길이 칩 등 모든 파라미터 칩에 설명 tooltip 존재
- [ ] 편집 화면 장시간 사용 중 화면 꺼짐 방지 (Wake Lock, 지원 브라우저)
- [ ] 요약 상세 모달 열려 있을 때 화면 꺼짐 방지 (Wake Lock, 지원 브라우저)
