import type { NecklineDef } from './types';

// Halter neck: narrow opening at center, ties at neck — shoulders exposed
export const halter: NecklineDef = {
  type: 'halter',
  label: '홀터',
  topY: 120,
  cutoutPath:
    'M 175,120 L 175,130 C 180,180 190,200 200,200 C 210,200 220,180 225,130 L 225,120 Z',
};
